<%= form_for :song, :url => songs_path do |f| %>
  <p>
    <%= f.label :lyric %>
    <%= f.text_field :lyric, placeholder: 'Party lyric!' %><br>
  </p>
  <p>
    <%= f.submit 'Save' %>
    <%= f.submit 'Cancel' %>

  </p>
<% end %>
